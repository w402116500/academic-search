# Application runtime
APP_ENV=development
LOG_LEVEL=INFO

# 本地账号认证。使用至少 32 个随机字符；真实密钥仅填写到项目根目录 .env。
AUTH_JWT_SECRET_KEY=
AUTH_JWT_ISSUER=academic-search
AUTH_JWT_ACCESS_TOKEN_MINUTES=60

# Host-to-container connections
DATABASE_URL=postgresql+asyncpg://academic_search:academic_search_dev@localhost:55432/academic_search
REDIS_URL=redis://127.0.0.1:6379/0
MILVUS_URI=http://localhost:19530
# 本地 Milvus 不需要 Token；托管 Milvus 或生产环境按服务商要求设置。
MILVUS_TOKEN=
MILVUS_COLLECTION_NAME=academic_document_chunks_l3_v1

# 多源检索候选和进度只在 Redis 中短期保存。
SEARCH_HTTP_TIMEOUT_SECONDS=10
SEARCH_MAX_CONCURRENT_PROVIDERS=3
SEARCH_SESSION_TTL_SECONDS=7200
SEARCH_PROVIDER_CACHE_TTL_SECONDS=1800
# 每次检索最多通过 DOI 内容协商补全多少条已纳入候选的格式中立题录。
SEARCH_CITATION_ENRICHMENT_LIMIT=25

# S3-compatible object storage. Use OSS/COS credentials and endpoint in deployment.
S3_ENDPOINT_URL=http://localhost:9000
S3_REGION=us-east-1
S3_BUCKET=academic-documents
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_FORCE_PATH_STYLE=true

# 全文获取：仅自动下载已明确开放获取的 HTTPS 直链 PDF。
FULLTEXT_DOWNLOAD_TIMEOUT_SECONDS=30
FULLTEXT_TOTAL_TIMEOUT_SECONDS=90
FULLTEXT_MAX_FILE_SIZE_BYTES=52428800
FULLTEXT_MAX_REDIRECTS=3
FULLTEXT_STAGING_PREFIX=staging/fulltext
# 仅当开放获取全文直连不可用时改为 proxy；代理地址复用 LITERATURE_PROXY_URL。
FULLTEXT_NETWORK_MODE=direct

# 研究对话模型。当前默认使用 DeepSeek；真实 API Key 只填写在根目录 .env。
WORKFLOW_CHAT_PROVIDER=deepseek
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_CHAT_MODEL=deepseek-chat

# RAG embedding 模型与可选的 OpenAI 兼容聊天后端配置。
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_CHAT_MODEL=gpt-4.1-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-large
# 首页研究要求解析的单次模型调用超时；超时后计划会标记失败，可修改后重新生成。
WORKFLOW_INTENT_TIMEOUT_SECONDS=45
# 候选相关性 Agent 使用流式 JSON。仅在连续 120 秒没有任何流活动时失败；
# 不存在整次调用总时长，也不按候选数量或摘要长度截断或拆批。
WORKFLOW_RELEVANCE_STREAM_IDLE_TIMEOUT_SECONDS=120
# 相关性 Agent 一次接收完整候选集合及完整摘要，不按候选数量阻断或拆批；
# 两次完整集合调用的输出预算分别按实际候选数计算。
WORKFLOW_RELEVANCE_OUTPUT_TOKENS_PER_CANDIDATE=700
WORKFLOW_RELEVANCE_VERIFICATION_OUTPUT_TOKENS_PER_CANDIDATE=128
# 检索运行配额：每次开始或重试都会消耗一次 UTC 自然日额度。
WORKFLOW_USER_DAILY_SEARCH_RUN_LIMIT=20
WORKFLOW_GLOBAL_DAILY_SEARCH_RUN_LIMIT=500

# RAG 文献入库：只有 L3 叶子片段写入 Milvus；token 数使用 tiktoken 精确统计。
RAG_MAX_L1_CHARACTERS=12000
RAG_MAX_L2_CHARACTERS=4000
RAG_MAX_L3_CHARACTERS=1200
RAG_L3_OVERLAP_CHARACTERS=160
RAG_TOKENIZER_ENCODING=cl100k_base
RAG_EMBEDDING_BATCH_SIZE=16
# 文献仅在确认构建并实际投递 Worker 后消耗入库额度；pending 不计入。
RAG_USER_DAILY_INGESTION_RUN_LIMIT=40
RAG_GLOBAL_DAILY_INGESTION_RUN_LIMIT=1000
# 可选 RAG 精排服务：三项必须同时设置；URL 不能只填服务根路径。
# SiliconFlow 示例：URL=https://api.siliconflow.cn/v1/rerank，模型=Qwen/Qwen3-Reranker-4B。
RAG_RERANKER_URL=
RAG_RERANKER_API_KEY=
RAG_RERANKER_MODEL=
