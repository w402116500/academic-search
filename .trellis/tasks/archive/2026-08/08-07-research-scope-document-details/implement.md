# 研究范围详情实施计划

1. 将研究范围触发器移至 `ResearchChatView` 的固定头部，保留现有抽屉与本地交互状态。
2. 复用工作区列表查询展示待完成删除恢复区，并复用删除 mutation 继续清理。
3. 在桌面与窄屏浏览器测试中验证范围入口、详情面板与恢复入口互不遮挡。
4. 运行相关单元测试、浏览器测试、格式化、lint 与类型检查。

## 回退点

- 若头部空间在窄屏不足，调整图标按钮布局，不把范围入口退回会被隐藏的侧栏。
- 不为待完成删除创建第二套查询、DTO 或页面本地持久状态。
