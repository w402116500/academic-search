# Task Plan: 研究入口到文献结果后端闭环

## Goal

将用户的自然语言研究要求接入后端并迁移到 Vue 3 前端，完成工作区草稿、意图分析、研究计划确认、多源异步检索、进度恢复、统一文献结果输出和集合构建，为后续 RAG 研究提供稳定入口。

## Current Phase

全部计划阶段已完成

## Current Objective

相关性 Agent 现以完整候选集合和完整摘要一次进入共享上下文；候选数量与摘要长度均不再触发截断、容量失败或串行分批。

## Phases

### Phase 1: 需求与边界确认

- [x] 阅读产品、检索、RAG、数据库和前端交互讨论稿
- [x] 对齐工作区创建时机：提交非空要求时创建 `draft`
- [x] 对齐已实现能力：Provider、准入服务、RAG 入库 Worker 已存在
- [x] 明确未实现能力：检索任务 API、自动投递、SSE、意图 Agent、RAG 检索与回答
- **Status:** complete

### Phase 2: 工作流契约与状态基础

- [x] 为工作区增加独立的研究阶段字段，避免与 `active/archived` 生命周期状态混用
- [x] 为每个工作流状态补充中文代码注释、数据库字段注释和 API 展示元数据，英文状态值保持稳定
- [x] 定义研究计划契约：原始问题、候选方向、时间范围、语言、查询表达式、确认版本
- [x] 定义检索运行契约：运行标识、阶段、Provider 状态、候选计数、错误、重试和完成状态
- [x] 明确 PostgreSQL 持久状态与 Redis 短期搜索状态的边界与 TTL
- [x] 为工作流状态转换、权限和重复提交补充单元测试
- **Status:** complete

### Phase 3: 创建工作区与意图分析

- [x] 扩展创建工作区接口，接收研究要求并保存原始问题
- [x] 实现结构化意图分析适配器，输出 2-3 个候选研究方向
- [x] 校验模型输出，拒绝缺少方向、范围或语言字段的非法结果
- [x] 实现读取、修改和重新生成研究计划的接口
- [x] 实现确认研究计划接口，固化时间范围、语言和查询计划
- [x] 测试模型超时、格式错误、重复确认和用户越权场景
- **Status:** complete

### Phase 4: 多源检索任务与进度

- [x] 由确认计划创建 `search_run`，防止未确认计划直接检索
- [x] 用 arq/Redis 投递检索任务，复用现有 Provider Registry 和限速配置
- [x] 并发执行 OpenAlex、Crossref、arXiv、Semantic Scholar，并隔离单源失败
- [x] 接入现有规整、去重、初筛、题录补全和引用格式化处理链
- [x] 保存短期候选结果、来源状态和可恢复错误，不将未准入候选写入长期 `papers`
- [x] 提供检索运行详情、候选结果和重试接口
- [x] 提供 SSE 进度事件：阶段、来源状态、数量、失败原因和完成状态
- [x] 编写离线 Provider 编排测试和真实多源集成测试
- **Status:** complete

### Phase 5: 全文准入与入库任务 API

- [x] 提供候选全文获取/校验接口，只接受当前搜索运行发现的 URL
- [x] 接入现有 Open Access PDF 下载与 MinIO 暂存服务
- [x] 提供加入、移出待确认集合接口，并复用严格 DOI/全文准入服务
- [x] 确认集合后自动投递 `ingestion_run_id` 到 arq
- [x] 提供集合文献、入库阶段和可问答数量查询接口
- [x] 提供部分失败、重试和失败原因恢复接口
- [x] 验证准入事务、对象补偿清理和 Worker 投递的端到端行为
- **Status:** complete

### Phase 5 Verification

- [x] 使用真实 arXiv 开放 PDF 完成“暂存 -> 准入 -> 构建 -> 解析分块 -> 向量化 -> Milvus 写入 -> 工作区可研究”的完整闭环。
- [x] 验证状态按 `pending -> queued -> running -> completed` 推进；完成的入库运行被标记为当前版本，工作区进入 `researching`。
- [x] 验证硅基流动 `Qwen/Qwen3-Embedding-0.6B` 实际返回 1024 维向量；PostgreSQL 记录 4 个 L1、13 个 L2、47 个 L3 分块，Milvus 写入 47 个 L3 向量。
- [x] 修复异步 SQLAlchemy 会话在只读查询后留下隐式事务、导致后续写事务无法开始的问题，并补充回归测试。
- [x] 按 UUID、对象键和入库运行精确清理真实测试数据，确认 PostgreSQL、MinIO 与 Milvus 均未留下临时记录。
- **Status:** complete

### Phase 6: 端到端验证与前端接入准备

- [x] 以静态原型页面的状态需求为依据整理 OpenAPI 契约
- [x] 为工作区切换器提供名称/阶段搜索和稳定游标分页
- [x] 完成“输入要求 -> 计划确认 -> 检索启动”的 API 集成测试
- [x] 验证刷新页面后可以恢复工作区、计划和检索运行状态
- [x] 验证跨用户、跨工作区候选与全文访问隔离
- [x] 验证 Provider 部分失败、任务重试和客户端断线恢复
- [x] 更新后端开发说明和接口文档，记录本链路的启动与测试命令
- **Status:** complete

### Phase 7: Vue 前端主流程实施

- [x] 阅读静态原型和前端交互讨论稿，建立页面、路由和服务端状态映射
- [x] 创建 Vue 3 应用壳、路由、TanStack Query 客户端、Pinia 轻量状态和统一 API 客户端
- [x] 实现登录、注册、当前用户和鉴权失效恢复
- [x] 实现研究入口：提交非空要求、分析状态、计划候选、时间范围/语言编辑和计划确认
- [x] 实现检索进度：来源状态、候选数量、失败原因、重试和 SSE 断线恢复
- [x] 实现结果表、论文详情、引用复制、全文获取、加入/移出待确认集合
- [x] 实现集合确认构建、入库阶段、部分失败和页面刷新恢复
- [x] 实现工作区切换器的关键词搜索、游标分页和当前阶段跳转
- [x] 为候选题录补充正式引用格式 API，并在详情页提供 GB/T、APA、MLA、Chicago 与 BibTeX 预览、复制
- [x] 为关键页面添加组件/组合式函数单元测试和 Playwright 主流程测试
- [x] 在真实 FastAPI、PostgreSQL、Redis 和 Worker 环境验证前端主流程
- **Status:** complete

### Phase 7.1: 原型交互流程对齐

- [x] 新增连续研究画布，将任务解析、计划确认和检索推进保留为一个用户可感知的流程
- [x] 将工作区阶段栏收敛为任务解析、文献筛选和证据研究三个主阶段
- [x] 移除会绕过当前阶段的侧栏平级导航，工作区切换只进入目标工作区的当前阶段
- [x] 重组候选筛选页，补齐处理明细、候选检查器和确认构建集合的明确边界
- [x] 更新前端路由映射、单元测试与 Playwright 主流程测试，并在 API 契约模拟环境验证
- **Status:** complete

### Phase 7.2: 真实联调状态修复

- [x] 修复重新进入工作区时未恢复当前检索运行、错误回到计划确认页的问题
- [x] 修复检索运行画布读取到过期候选统计、完成后仍显示 0 条候选的问题
- [x] 修复未就绪题录被错误显示为“正在渲染”的永久加载状态
- [x] 将全文 `rejected` 识别为终态，并在未满足题录准入时禁用全文请求入口
- [x] 为上述状态分支补充前端测试，并用真实 FastAPI 环境复验已有工作区
- [x] 复跑浏览器真实回归，确认移除无全文状态探测后不再产生 404 控制台错误
- **Status:** complete

### Phase 8: RAG 研究会话后端

#### Phase 8.1: 会话、运行与接口契约

- [x] 审计既有 `conversations`、`messages`、`research_runs`、`research_evidences` 表与已应用迁移，补齐仅在实际 API 需要时才存在的约束、索引和中文注释
- [x] 定义会话、消息、研究运行、执行阶段、证据引用与“证据不足/需要澄清”Pydantic 契约；机器状态保持英文稳定值，接口提供中文展示元数据
- [x] 设计会话创建、列表、读取、提交问题、读取运行、重试和取消 API；所有资源均通过工作区所有权与集合范围校验
- [x] 明确 PostgreSQL 业务运行、LangGraph checkpoint、Redis 进度流各自保存的状态，避免把恢复能力放入进程内存或消息 JSON
- [x] 为权限隔离、重复提交、运行状态转换、会话删除与输入校验编写单元及 API 契约测试
- **Status:** complete

#### Phase 8.2: 受限混合检索与证据组装

- [x] 扩展现有 OpenAI 兼容 embedding 适配器，支持查询向量且校验其维度与当前 Milvus collection 一致
- [x] 实现检索仓储：先由 PostgreSQL 确定当前 `completed/is_current` 文档版本，再以 `owner_user_id + collection_id + ingestion_run_id + level=3` 过滤 Milvus；读取原文时再次校验集合和版本
- [x] 实现关键词召回、向量召回、RRF 融合、同文献父块 Auto-merging 与可配置的相关性阈值；先以可审计的确定性算法落地，再只在有真实评估数据时引入 Reranker
- [x] 为每个候选保存检索阶段、分数、父块合并原因、页码/章节定位和最终引用片段；不把检索分数误表示为事实正确性
- [x] 用内存替身和本地 Milvus/PostgreSQL 验证跨用户、跨集合、旧文档版本和无可检索文献均无法泄漏内容
- **Status:** complete

#### Phase 8.3: 单轮证据回答 LangGraph 与异步运行

- [x] 实现 `single_rag` LangGraph：问题规范化 -> 受限检索 -> 证据充分性判断 -> 带引用回答 / 一次改写 / 澄清 / 拒答
- [x] 查询改写最多一次；HyDE 或 Step-back 仅用于检索，不能成为引用或答案事实来源
- [x] 以结构化模型输出约束回答引用只能来自本次 `research_evidences`；每条事实、方法、结果或结论均关联论文信息、原文摘录和页码或章节定位
- [x] 通过 arq 独立研究 Worker 执行运行，持久化 `queued -> running -> awaiting_clarification/completed/failed/cancelled`，向 Redis Stream 发布可展示阶段事件
- [x] 接入 LangGraph PostgreSQL checkpoint，实现 Worker 重启后的同一运行恢复；checkpoint 不替代 `research_runs` 业务审计记录
- [x] 对模型、嵌入、Milvus、数据库与 checkpoint 失败建立稳定错误码、可重试规则和取消边界
- **Status:** complete

#### Phase 8.4: 复杂研究问题与证据核验

- [x] 为跨论文比较、冲突分析和结构化研究笔记定义受限的复杂问题判定契约、子问题上限、并发上限、超时与总模型预算
- [x] 使用 Plan-and-Solve 生成有限子问题；每个子问题只可调用 Phase 8.2 的集合受限检索
- [x] 增加证据核验步骤，拒绝片段不蕴含、扩大解释和无法定位的原子事实；综合节点只能消费已核验事实
- [x] 使用受限 ReAct 工具循环，工具仅限检索与读取已授权证据；达到工具或改写预算后必须结束为回答、澄清或证据不足
- [x] 保存子问题、检索 trace、核验结论和未解决缺口，支持部分成功但不允许其他子问题静默猜测补齐
- **Status:** complete

#### Phase 8.5: 真实端到端验收与质量基线

- [x] 使用真实已准入、已向量化的论文集合完成单篇问答和证据不足离线图测试；跨论文比较、部分入库失败与刷新恢复仍需多文献真实样本
- [x] 核验每个最终引用都能回到同一用户、同一集合、当前文档版本的 PostgreSQL 片段，并可显示题名、作者/年份、链接及页码或章节
- [~] 验证 Worker 重启、模型/向量服务瞬时失败、重复投递、取消、无权限访问和 Redis 事件断线恢复（已验证无权限访问、Redis `Last-Event-ID` 断线恢复、队列重复投递的幂等代码路径；Worker 重启、显式取消和模型/向量瞬时失败仍需专项真实故障注入）
- [x] 记录首批检索命中、引用可定位和证据支撑人工验收样本；单轮基线已满足启用后续复杂模式的代码条件
- **Status:** in_progress

### Phase 9: Vue 研究对话页接入

- [x] 将集合页的对话入口仅在当前集合可问答文献数大于零时解锁，并传递服务端工作区与集合标识
- [x] 接入真实会话列表、创建会话、消息流、运行状态和 SSE 断线恢复；不在前端伪造研究回答
- [x] 在每条回答下展示可展开证据、论文题名、作者/年份、原文摘录和页码或章节定位；证据不足和澄清属于正常可见状态
- [x] 实现当前工作区内会话历史、明确的范围切换、刷新恢复和会话删除确认；工作区切换不与会话侧栏相互挤占
- [x] 通过真实 FastAPI、Redis、Worker、PostgreSQL 和 Milvus 浏览器回归，覆盖引用定位、越权拒绝、长任务恢复和移动端可读性
- **Status:** complete

### Phase 10: 候选相关性评估 Agent

#### Phase 10.1: 评估契约与边界

- [x] 定义后端唯一的 `CandidateRelevanceAssessment` 契约，包含相关层级、自然语言理由、研究内容概述、可帮助的研究环节、局限、建议和逐条证据。
- [x] 将相关层级限定为 `core`（核心相关）、`related`（关联研究）、`background`（背景参考）、`not_recommended`（不建议优先）和 `insufficient_information`（信息不足）；这些不是论文质量分数，也不替代用户最终决定。
- [x] 证据仅允许引用候选的标题或摘要；每条证据记录来源字段、原文摘录和可选的字符位置。服务端必须验证摘录确实来自对应原文，拒绝模型编造的引文。
- [x] 明确摘要缺失、题录冲突或模型无法判断时的行为：返回“信息不足”或明确失败，绝不回退为前端关键词推测。
- [x] 在 `UnifiedCandidate` 的 Redis 短期快照中保存评估结果与状态；评估数据不写入长期论文表。仅为公开搜索运行阶段新增约束迁移，不建立候选评价表。
- **Status:** complete

#### Phase 10.2: 检索 Worker 中的评估阶段

- [x] 在多源规整、去重和规则初筛完成后，先发布带“待评估”状态的候选快照，再为 `triage.included=true` 且具备摘要的候选增加“相关性评估中”阶段；缺摘要候选由确定性规则标为信息不足，未通过基础初筛的记录不消耗模型调用。
- [x] 复用现有 DeepSeek/OpenAI 兼容 LangChain 模型适配器，以受控批次评估候选；输入只包含用户原始问题、已确认方向、时间/语言范围、实际检索词以及候选元数据。
- [x] 对单次输入的候选数量、摘要长度、批次并发、超时和最大输出长度设定明确上限，避免 50 条以上候选导致不可控成本或模型上下文溢出。
- [x] 使用结构化输出和服务端二次校验：返回候选 ID 必须完整且唯一；层级、证据字段、证据摘录和推荐文案均须符合契约。
- [x] 允许批次级部分失败：已完成候选保持结果，失败候选显示明确失败原因并可针对失败项重试；不因为单个来源或模型调用失败而覆盖已有候选。
- [x] 将 Search Run 的公开阶段、Redis Stream 事件和恢复快照同步扩展到该阶段，使刷新页面或 SSE 断线后可看到已评估、待评估和失败数量；候选结果页在快照可用时进入，不等待整轮模型调用结束。
- [x] 将当前“检索完成前批量题录补全”的实现改为非阻塞策略：仅对 Agent 已判定为核心/关联的少量候选预取题录，其余在用户打开详情、复制引用或加入集合时按需处理。
- **Status:** complete

#### Phase 10.3: API 与短期状态恢复

- [x] 扩展候选结果 API，使其直接返回服务端评估结果、评估状态和可展示错误；前端不再自行推断相关性。
- [x] 设计失败候选的授权重试 API，只允许当前工作区所有者、当前搜索运行且处于可重试状态的候选触发；重复请求必须幂等。
- [x] 维持现有 Redis TTL 边界：候选及评估随搜索会话过期；过期后提示重新检索，不把模型评价错误地当作长期学术结论保存。
- [x] 确认候选被加入研究集合时仅沿用既有 DOI、正式题录和全文准入边界；相关性评估只能辅助排序与人工筛选，不能绕过准入。
- **Status:** complete

#### Phase 10.4: 前端理由呈现与旧规则退役

- [x] 删除生产路径对 `candidate-reason.ts` 关键词匹配规则的依赖，确保候选理由只有一个服务端事实来源。
- [x] 列表卡片只展示层级标签和一句“为什么保留”的自然语言摘要；不显示虚假的百分比、命中次数或模型内部技术字段。
- [x] 右侧检查器按“它主要研究什么 / 对当前研究有什么帮助 / 需要留意 / 判断依据 / 建议”展示评估结果，并让标题、摘要证据可展开核对。
- [x] 对评估中、信息不足和评估失败分别提供清晰的中文状态；失败状态提供显式重试，不伪造关键词理由作为降级内容。
- [x] 保留用户自主选择：低优先级与不建议优先的候选默认不自动入选、不自动删除，用户可按优先审核、背景参考和需人工核对筛选后决定。
- **Status:** complete

#### Phase 10.5: 质量验证与真实验收

- [x] 编写契约和服务单元测试，覆盖模型输出缺字段、候选 ID 重复、证据不属于原文、无摘要、批次部分失败和重试幂等。
- [x] 编写搜索 Worker/API 集成测试，覆盖 Redis 快照恢复、SSE 进度、跨用户隔离、搜索会话过期和现有全文准入不被评估结果绕过。
- [x] 更新前端单元与 Playwright 测试，验证理由来自后端、证据可查看、失败不会显示伪理由、筛选不会改变用户选择结果。
- [x] 使用显式环境变量完成 DeepSeek 结构化评估验收，并以受控的真实多源检索验证候选渐进评估；模型理由证据均可回到候选标题或摘要，临时数据库和 Redis 数据已清理。
- [x] 完成后运行后端测试、Ruff、Pyright、Alembic check，以及前端 typecheck、lint、unit、build 和浏览器主路径回归。
- **Status:** complete

### Phase 11: 发布前审计、文档同步与版本提交

- [x] 恢复并审查 Phase 10 的统一候选、Redis 快照、模型契约、API/SSE 与前端呈现边界，确认候选理由没有回退到前端关键词推断。
- [x] 核对讨论稿、环境模板与后端开发说明均记录“规整初筛 -> 候选快照 -> 相关性评估 -> 高相关题录预取”的真实处理顺序。
- [x] 复跑后端、迁移与前端全量静态/测试验证；所有命令须使用项目锁定的 Python、Node 20.19.6 与 pnpm 10.34.5。
- [x] 复查完整暂存差异、暂存迁移文件的最终格式和空白字符，确认没有真实密钥、临时数据或未说明的行为变更。
- [x] 创建包含研究工作流、RAG 对话、Vue 接入和候选相关性评估的本地 Git 提交；提交标识以最终 `git log -1` 为准。
- **Status:** complete

### Phase 12: 候选审核与集合准备交互改造

#### Phase 12.1: 交互契约与状态边界

- [x] 将“正在查看的候选”“本次准备清单”“待确认集合”“可研究集合”定义为四种不同对象；检查器焦点只存在于前端，准备清单随当前 `search_run` 的 Redis 会话保存，待确认与可研究集合继续由 PostgreSQL 持久化。
- [x] 将检查器中的“当前选择”统一改为“正在查看”，避免与多选候选混淆；不再使用禁用的“确认 0 篇入集合”作为页面主操作。
- [x] 确定准备清单的准入边界：缺少 DOI 的候选可查看、复制和人工核对，但不能被选择用于 RAG 集合；其余候选可进入准备清单，并由后续题录/全文核验决定能否真正入集合。
- [x] 明确跨页选择、搜索筛选、刷新恢复、搜索会话过期、重新检索与已有待确认集合并存时的行为，防止新搜索运行覆盖旧的持久集合。
- **Status:** complete

#### Phase 12.2: 候选会话 API、批量核验与分页

- [x] 在 Redis 搜索快照中保存当前用户的准备清单候选 ID，并由服务端返回每篇候选的准备选择状态和各准入状态统计；不增加长期“候选收藏”表。
- [x] 提供授权且幂等的候选选择接口，以及“批量准备核验”和“批量加入待确认集合”接口；每个候选独立返回题录、全文和准入结果，单项失败不能回滚其余成功项。
- [x] 将候选列表改为稳定排序的游标分页接口，默认每页 20 条、上限 50 条；语言、相关性、全文状态、DOI、已选择等筛选条件与游标必须在同一服务端快照上计算。
- [x] 复用现有单篇题录补全、全文下载、全文准入和 arq 队列；批量接口只负责编排已授权候选，不绕过 DOI、正式题录和可处理全文的既有边界。
- [x] 为候选会话 TTL、跨工作区访问、重复点击、跨页选择、无 DOI、题录冲突、全文失败、会话过期和批量部分成功补齐后端测试。
- **Status:** complete

#### Phase 12.3: 结果页、检查器与集合确认体验

- [x] 结果表增加独立复选框列，行点击仅切换检查器；筛选栏下方仅在至少选择一篇时出现粘性选择工具条，展示已选数量、可核验数量、可加入数量与不可准入原因。
- [x] 工具条提供“本页全选”“只看已选”“准备核验”“加入待确认集合”和“清空选择”；所有文案必须说明操作发生在本次准备清单、待确认集合或最终构建中的哪一层。
- [x] 用传统分页承载候选审核，显示当前页、总数、每页数量和上一页/下一页；跨页、筛选和刷新后保持选择，避免无限滚动造成审阅进度丢失。
- [x] 检查器保留单篇理由、证据、题录、全文和独立操作；根据状态显示“准备全文核验”“全文核验中”“加入待确认集合”“已加入待确认集合”或具体不可用原因。
- [x] 将顶部入口改为“待确认集合 N 篇”；仅在 N 大于零时打开集合确认抽屉，抽屉列出实际可构建文献、移除操作、构建影响与唯一的“确认构建 N 篇文献”主按钮。
- [x] 为加载、批量部分失败、会话过期、空筛选结果、移动端表格收敛、键盘多选与减少动态效果提供明确的可访问状态。
- **Status:** complete

#### Phase 12.4: 端到端验收、讨论稿与提交

- [x] 编写前端单元测试与 Playwright 流程，覆盖跨页选择保持、筛选后选择保持、批量状态分流、单篇检查器不改变多选、待确认集合数量同步和刷新恢复。
- [x] 已用真实 FastAPI、Redis、MinIO、arXiv 开放 PDF、Workflow Worker 与批量准入服务验收“选择 -> 批量核验任务 -> 就绪全文 -> 加入待确认集合”，并精确清理临时资源；代理模式下专项测试 `1 passed in 26.69s`。
- [x] 已更新 `03`、`04`、`07`、`08`、`10` 号讨论稿及后端/前端开发说明，使接口、交互文案和严格准入规则一致；完成格式、类型、测试、构建、差异审计与 Git 提交前检查。
- **Status:** complete

### Phase 13: 检索等待页可验证进度体验

- [x] 审计 SSE 已有的来源、候选、相关性总数/完成数/失败数与前端缺失状态，定义只消费真实数据的展示契约。
- [x] 改造检索运行画布：增加候选相关性阶段、实时完成计数、最近更新反馈、延迟来源解释与可恢复提示；不引入虚假总时长或百分比。
- [x] 调整浅色学术工作台的排版、动效与可访问状态，保证 `prefers-reduced-motion` 下仍可理解当前进展。
- [x] 补充状态辅助函数和浏览器流程测试，覆盖相关性推进、来源部分失败及完成后的筛选交接；运行中无新事件超过 15 秒会显示手动重连提示。
- [x] 同步前端体验/交互讨论稿，完成前端静态检查、单元、浏览器和生产构建验证。
- **Status:** complete

### Phase 14: 文献核验任务页与待确认集合衔接

- [x] 审计现有候选审核、全文任务、准备清单与待确认集合 API，确认可展示的真实状态、分页与可复用动作；不为视觉补造虚假步骤。
- [x] 定义核验任务页路由与状态契约：本次准备清单、逐篇题录/全文状态、可加入数、已加入待确认集合数、处理中数和受阻原因。
- [x] 实现独立 Vue 核验任务页及从结果页的连续跳转。页面应保留返回候选审核的入口，并使用真实服务端状态驱动刷新或轮询。
- [x] 将“加入待确认集合”放在核验页的可加入文献范围内：支持只加入当前已就绪项、完成后回写每项状态和集合摘要，其他文献继续处理。
- [x] 依据现有浅色学术工作台设计系统完成视觉审计与美化：强化任务进展层级、加载/空/失败/完成和窄屏状态，图标动作增加可理解文本或无障碍说明。
- [x] 补充单元、浏览器流程与讨论稿，执行前后端相关检查、差异审计；提交 Git 仅在用户再次明确要求时进行。
- **Status:** complete

### Phase 15: 实施对齐整改 P0

- [x] 审计候选相关性调用、Redis 快照、SSE 统计和候选审核游标的当前契约，并先将集合级输入、完整覆盖输出与失败策略写成可执行测试。
- [x] 将相关性评估改为一次接收完整 `eligible_candidates` 集合；保留无摘要的确定性“信息不足”和单条输出校验，不以隐式串行批次代替容量策略。
- [x] 为超过显式集合容量的检索定义可恢复、用户可见且不伪造集合级判断的状态；同步 API/SSE 计数与重试边界。
- [x] 将终态候选排序改为相关性层级优先，同层使用稳定、可解释的辅助键，再使用年份、标题规范化键和候选 ID；游标绑定此排序和筛选语义。
- [x] 更新候选审核服务、工作流和 API 契约测试，覆盖完整集合单次调用、缺失/重复 ID、容量边界、终态排序与游标稳定性。
- [x] 将 README 与讨论稿状态同步为真实能力和明确缺口，不将计划或模拟行为描述为已实现能力。
- [x] 完成后端定向/全量测试、Ruff、格式、Pyright、Alembic check；若公开契约变化，再复验前端静态、单元、浏览器与生产构建。
- **Status:** complete

### Phase 15.1: P0 真实模型与基础设施验收

- [x] 扩展受环境变量保护的真实模型验收，使同一次调用包含多个统一候选，并验证全部候选的标题/摘要证据回链。
- [x] 运行真实模型验收；不得将模型密钥、原始响应或临时测试标识写入源码、讨论稿或最终报告。
- [x] 检查本地 PostgreSQL、Redis、MinIO 与 Worker 可用性；在服务可用时运行 P0 真实检索/审核专项，不操作既有用户数据并精确清理临时资源。
- [x] 记录命令、结果、跳过原因和清理结果；只有真实执行成功后才更新 P0 验收状态。
- **Status:** complete

### Phase 15.2: P0 当前环境真实复验

- [x] 重新执行多候选集合级相关性真实模型验收，确认本轮环境、模型配置与标题/摘要证据回链仍然可用。
- [x] 定位本地基础设施的实际启动方式，检查 PostgreSQL、Redis、MinIO 与 Milvus 的当前可用性。
- [x] 重新执行真实 arXiv PDF 下载、校验、MinIO 暂存/转正、Redis 准备清单与 PostgreSQL 准入专项；测试结束后检查其临时资源清理边界。
- [x] 两条真实专项均通过后记录当前复验结果；未将尚未实施的 P1/P2 标记为已完成。
- **Status:** complete

### Phase 16: 实施对齐整改 P1

- [x] 审计候选理由、全文采集、候选审核、RAG 回答与检索链路的当前边界，并将 P1 的新增证据、授权、能力与失败语义写成离线契约。
- [x] P1-A：为候选理由增加独立的主张—标题/摘要证据核验；主张核验缺失、越界或不支持时拒绝该条候选理由，不展示未经核验的结论。真实模型诊断已确认核验器会拒绝摘要外的“保护作用/机制/发表偏倚/残余混杂”等表述；收紧第一轮生成契约后，真实两候选集合专项 `1 passed in 17.62s`，两条均在独立核验后完成。
- [x] P1-A：在单轮 RAG 回答生成后增加原子主张—已引用片段的独立核验节点；任一主张不支持时回到证据不足，不持久化原回答。真实 Worker 专项已在当前环境完成 embedding、Milvus 检索、DeepSeek 回答与核验（4 条主张、0 条不支持），临时资源已精确清理；真实用例现断言该 trace。
- [x] P1-B：实现当前用户、当前候选、显式授权声明约束下的 PDF 二进制流暂存入口；复用 DOI/题录一致性、大小、类型、PDF 魔数、哈希、对象转正与严格准入服务。当前环境真实 MinIO/Redis/PostgreSQL 专项 `1 passed in 5.17s`，随机候选对象、Redis 准备清单与数据库记录均已清理。
- [x] P1-C：接入配置驱动、可替换的真实 HTTP Reranker；未配置时在 retrieval trace 和研究界面明确标识未启用，且不产生 `rerank_score`。HTTP 适配器和离线契约已通过，真实服务专项以 `Qwen/Qwen3-Reranker-4B` 验证中文候选的有效下标、有限分数和语义排序；真实 RAG Worker 启动路径会在启用配置后注入该适配器。
- [x] P1-C 配置入口已写入实际 `.env`：`RAG_RERANKER_URL`、`RAG_RERANKER_API_KEY`、`RAG_RERANKER_MODEL` 均由设置层校验。配置已改为完整 `/rerank` 路径和正确变量映射，安全存在性检查确认三项均非空，配置值和凭据不记录到规划文件。
- [x] 已完成 P1-A/P1-B/P1-C 的离线契约、真实基础设施或真实外部服务、前端状态与讨论稿验收；前端最终回归为 Prettier、vue-tsc、ESLint、Vitest（19 passed）、Playwright（8 passed）、生产构建及 `git diff --check` 全部通过。P1-C 后端全量回归为 Ruff、Pyright 和 pytest（177 passed, 15 skipped）通过。
- **Status:** complete

### Phase 17: 实施对齐整改 P2

- [x] 完成 P2 现状审计：复杂模式仍由关键词触发，子问题检索是一次性并行执行；运行中不能取消；尚无用户配额、全局预算或阶段指标的持久化契约。
- [x] 将复杂问题路由替换为结构化判定，并记录可理解的路由依据；定向图测试已通过。
- [x] 实现受工具和预算约束、可审计的复杂研究循环；控制动作、模型/检索预算和逐轮 trace 已落地。
- [x] 让运行中取消写入持久状态，并在 Worker 节点与外部调用边界协作停止；停止后不持久化回答或证据。
- [x] 加入用户配额、全局预算、阶段耗时/失败指标和故障注入验收；研究问答的服务层契约、真实 PostgreSQL + Redis + Worker 专项及前端浏览器验收已通过。检索和集合构建入库已补齐同类用户/全局 UTC 日预算、429 拒绝契约、批量预检和真实 PostgreSQL 验收；全量回归与讨论稿同步完成。
- **Status:** complete

### Phase 18: 意图分析失败恢复

- [x] 复核失败截图、前端页面、计划重生成 API、Worker 和意图分析适配器，确认失败发生在 RAG 前的研究计划生成阶段。
- [x] 以不持久化的真实模型探针验证当前 DeepSeek 意图分析可用；同一研究要求返回 3 个方向与 3 份查询计划。
- [x] 将失败页操作改为调用既有 `POST /plan/regenerate`，以原始研究要求创建新版本并显示重新生成中的状态。
- [x] 增加浏览器回归，断言失败计划点击恢复后发送正确请求并进入 `generating` 状态；完成静态检查和真实页面复验。
- **Status:** complete

### Phase 19: 真实浏览器全链路验收

- [x] 确认使用 Playwright CLI、已启动的真实前端/API/Worker，以及与用户数据隔离的临时账号策略。
- [x] 在浏览器中注册临时账号并提交指定研究要求，核验意图分析结果与研究计划确认。
- [x] 通过前端确认计划、观察真实多源检索和候选审核状态，并选择符合准入条件的文献进入集合准备。
- [x] 在真实文献可准入时通过前端构建集合、等待入库完成并提交研究问题；若任何外部源缺少可准入全文，记录准确阻断状态和前端恢复路径。
- [x] 保存必要截图，清理临时工作区和测试账号关联的本地资源，记录最终结果。
- **Status:** complete

### Phase 20: 项目重启与健康检查

- [x] 盘点并精确停止可归属的项目 Worker 与 Vite 进程；原 `8000` 监听 PID 无法由 Windows 进程表安全归属，保留该健康实例并启动受控替代 API。
- [x] 使用 Docker Compose 重启 PostgreSQL、Redis、etcd、Milvus 与 MinIO，并确认全部健康。
- [x] 启动受控 FastAPI（`8002`）、工作流 Worker、入库 Worker、研究 Worker 与 Vite 前端（`5173`）。
- [x] 对 API、前端和基础服务执行健康检查，记录监听端口与结果。
- **Status:** complete

### Phase 21: 候选相关性评估讨论稿复核

- [x] 定位讨论稿中关于完整候选集合、批次与进度展示的约束。
- [x] 对照当前容量配置和执行实现，识别与讨论稿及当前模型能力不一致的部分。
- [x] 移除候选数量阻断，按完整集合规模计算两次模型调用的输出预算，并同步测试、环境模板和讨论稿。
- [x] 使用当前真实模型验收 50 条临时内存候选：完整集合判断与独立核验均通过，未写入持久化资源。
- **Status:** complete

### Phase 22: 完整候选摘要输入对齐

- [x] 删除单篇摘要字符上限及相关环境配置。
- [x] 更新相关性 Agent 输入、单元测试与正式文档，确保完整摘要进入单次集合调用。
- [x] 复跑质量检查并重启工作流 Worker。
- **Status:** complete

### Phase 23: 讨论稿复核与无上限集合调用确认

- [x] 复读检索与实施对齐讨论稿，确认候选语义判断和理由核验均为集合级任务。
- [x] 审计运行代码、配置模板和测试，确认不存在候选数量上限、摘要截断或隐式串行分批。
- [x] 重新运行集合级回归测试，并核验当前 Worker 运行状态。
- **Status:** complete

### Phase 24: 开发运行环境重启

- [x] 定位并停止当前前端、API 和三类 Worker 的可归属进程树。
- [x] 重启 Docker 基础设施和所有应用进程。
- [x] 验证前端、API、CORS 和 Worker 进程均恢复健康。
- **Status:** complete

Phase 24 notes: 本地执行策略拒绝 `Stop-Process` 与 `Start-Process`，改用已核实 PID 的 `taskkill /T` 和无窗口 `cmd start /b` 完成受控进程重启。首个 Vite 进程从项目根目录启动并返回 `404`；已按前端目录重启，现为 `200`，且编译模块确认使用受控 API `http://127.0.0.1:8002`。未终止无法可靠归属的旧 `8000` 监听。

### Phase 25: 当前检索运行的相关性失败诊断

- [x] 读取运行审计、Redis 快照与候选错误码，确定失败阶段和范围。
- [x] 关联当前 Worker 和模型配置，定位可复现的根因。
- [x] 使用当前模型和同一完整候选输入执行无持久化写入的真实流式 JSON 探针。
- [x] 读取运行审计、Redis 快照与候选错误码，确定失败阶段和范围。
- [x] 关联当前 Worker 和模型配置，定位可复现的根因。
- [x] 使用当前模型和同一完整候选输入执行无持久化写入的真实流式 JSON 探针。
- [x] 用专用流式 relevance Worker 替代总时长模型调用和检索 Worker 内联等待。
- [x] 改为运行级全量重试、显式取消与候选快照字段级合并，退役单候选重试。
- [x] 完成定向、真实模型与浏览器全链路验证。
- **Status:** complete

### Phase 26: 运行时 Worker 版本诊断

- [x] 核对 `test@qq.com` 当前搜索运行的数据库审计、Redis 快照和事件流。
- [x] 区分候选状态、稳定错误码与 Provider 结果，定位 41 条重试候选的共同失败点。
- [x] 核对当前源码的 relevance 分派路径与实际 workflow Worker 的启动时间、队列记录。
- **Status:** complete

### Phase 27: 受控应用进程重启

- [x] 精确停止当前前端、API 与四类 Worker 进程树，不停止健康的基础设施容器。
- [x] 以当前源码重新启动应用进程，并确认 workflow Worker 已加载流式相关性分派逻辑。
- [x] 验证前端、API、CORS 与 Worker 监听状态。
- **Status:** complete

### Phase 28: 单篇相关性核验失败诊断

- [x] 核对当前运行的 Provider、流式相关性阶段和候选状态统计。
- [x] 定位唯一失败候选及其稳定错误码，并确认是否属于超时、模型不可用或证据核验拒绝。
- **Status:** complete

### Phase 29: 本轮工作区变更提交

- [x] 审计所有已跟踪与未跟踪改动，排除生成物、临时数据与密钥。
- [x] 执行最终差异和质量检查，并创建一个完整的本地 Git 提交。
- **Status:** complete

## 2026-08-02 实施对齐

已落地并待真实验收的后端实现：`c3e7a1b9d426` 迁移、研究会话 API、受限混合检索、LangGraph、ARQ 研究 Worker、Redis Stream 和 PostgreSQL checkpoint。Phase 8.1 至 8.4 的代码和离线测试均已完成；本轮以 Phase 8.5 和 Phase 9 的真实验收及前端接入为执行范围。

## 2026-08-03 — 完整 RAG + Reranker 真实链路复验

- [x] 以当前真实配置执行已入库临时文献的完整 RAG Worker 用例，覆盖 Qwen embedding、Milvus 受限召回、HTTP Reranker、DeepSeek 回答、回答主张核验、PostgreSQL、Redis 事件和精确清理；真实专项通过（2 条证据、Reranker 2/2、4 条已支持主张、7 个事件、临时资源已删除）。
- [x] 将真实用例收紧为：Reranker 必须启用，trace 必须为 `completed/http_reranker`，且候选数、返回数有效；不能以 RRF 禁用分支通过本轮验收。
- **Status:** complete

- [x] 保持已区分的“搜索运行”和“研究运行”类型边界，并增加独立 `/research/:workspaceId` 路由。
- [x] 实现集合页解锁入口、研究会话侧栏、问题提交、SSE 阶段恢复、回答证据展开和删除确认。
- [x] 完成前端静态检查、后端完整检查，以及真实 PostgreSQL、Redis、Milvus、ARQ、模型的单篇验收；复杂模式保留真实多文献故障基线待补。

已完成真实单篇验收：`RUN_LIVE_RESEARCH_E2E_TESTS=1 uv run pytest tests/integration/test_live_research_e2e.py -m live -s` 通过。真实 Qwen embedding 写入 Milvus 后，受限检索命中 2 个 L3 片段，DeepSeek 生成带 2 条可回链引用的回答，Redis 产生 5 个公开阶段事件；临时 PostgreSQL、Milvus、Redis 与 checkpoint 资源均已清理。复杂模式和证据不足分支已有离线图测试，待后续真实多论文集合基准验证。

2026-08-02 本轮补充真实 API + arq 验收：临时账号经 FastAPI 提交研究要求，DeepSeek 意图分析生成 3 个方向；确认计划后 workflow Worker 实际完成 OpenAlex、Crossref、arXiv 检索，API 返回 51 条候选、50 条纳入候选和 25 条题录补全。另一条临时已索引集合经研究会话 API 投递问题，research Worker 实际完成 Qwen 查询嵌入、Milvus 受限召回和 DeepSeek 回答；最终 API 返回 `completed`、2 条可定位证据，Redis Stream 产生 5 条公开事件且 SSE 可返回初始快照。全文入库真实验收同时确认 arXiv PDF -> MinIO -> 47 个 L3 分块 -> 47 条 1024 维 Milvus 向量 -> `researching` 状态闭环。所有临时资源均已清理。

## Key Questions

1. 研究计划和检索运行中哪些字段需要长期写入 PostgreSQL，哪些只在 Redis TTL 内保留？
2. 工作区研究阶段是新增 `workflow_stage` 字段，还是使用独立的工作流运行表承载？
3. 意图分析首版使用一次结构化 OpenAI 兼容模型调用，还是直接用 LangGraph `SearchGraph` 封装？
4. 搜索候选在 Redis 过期后，前端应提示重新检索，还是将候选结果持久化为可恢复的搜索快照？
5. 前端首版是否只支持桌面端完整工作流，还是同步保证移动端的结果查看与状态恢复？

## Decisions Made

| Decision                                               | Rationale                                                                                                 |
| ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------- |
| 先实现“入口到结果”垂直闭环                             | 前端已有连续流程，Provider 适配器已完成，缺的是可恢复的后端编排                                           |
| 提交要求时先创建 `draft` 工作区                        | 与最新前端交互稿一致，保证离开页面后仍有任务边界                                                          |
| 意图分析输出结构化研究计划                             | 用户需要确认方向、时间和语言，检索不能直接依赖自然语言模型文本                                            |
| 状态值英文、说明中文                                   | 英文值适合作为数据库/API 稳定标识；中文注释和展示元数据帮助开发者与用户理解状态                           |
| Provider 调度和任务状态由确定性代码负责                | 限速、重试、权限和状态需要可预测、可审计，不能交给 Agent 自主循环                                         |
| 未通过准入的候选只保留短期搜索状态                     | 数据库长期 `papers` 只保存 DOI、正文和权限均已核验的文献                                                  |
| RAG 检索与研究对话放在本链路之后                       | 必须先有可研究集合和完成的向量索引，避免提前实现无数据可用的聊天接口                                      |
| 检索候选详情留在 Redis                                 | `search_runs` 只持久化运行状态、统计、错误与 Redis 会话键，过期候选可由确认计划重新生成                   |
| 入库 Worker 同一 `AsyncSession` 在只读后无法开启写事务 | 1                                                                                                         | 在仓储层将查询结果转换为值对象后结束只读事务，使后续状态与向量记录写入拥有明确事务边界 |
| 前端主流程需要等待 RAG 对话 API                        | 研究集合构建已有完整后端接口，但研究检索、回答和证据 trace 尚未提供；先实施前置流程，避免前端伪造研究回答 |
| 候选引用格式需要专用 API                               | 后端已有格式化器但没有候选题录渲染端点；前端不能手拼格式或把标题加 DOI 当作正式引用                       |
| 前端必须将题录与全文准入失败清楚呈现                   | `citation_not_ready` 是既定业务门槛，不应以永久加载或请求按钮掩盖服务端拒绝结果                           |
| 候选相关性与研究准入分离                               | Agent 只判断标题/摘要与研究意图的关系；DOI、题录和全文状态是确定性准入信号，二者同时展示但不能互相覆盖    |
| 候选快照优先于语义评估完成                             | 用户不应等待全部模型调用才能看到文献；Redis 快照和 SSE 逐批更新，前端只呈现服务端状态                     |

## Errors Encountered

| Error                                                             | Attempt | Resolution                                                                                                                                                            |
| ----------------------------------------------------------------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 前端读取 `Record<string, unknown>` 的预算 trace 未先缩窄          | 1       | 增加 `isTraceRecord` 守卫后再读取调用次数和耗时字段，保持服务端审计数据的未知键安全                                                                                |
| 运行中取消分支末尾缺少显式返回，Pyright 报告可能返回 `None`       | 1       | queued/running 分支后无条件抛出稳定冲突错误，保证服务响应路径完整                                                                                                  |
| 复杂循环在耗尽计划查询后自动收束，未记录最终回答决策              | 1       | 让控制器在剩余查询为空时也显式选择 `answer` 或 `clarify`；每轮决策都保留在 trace                                                                                |
| P2 受限循环的 lambda 捕获循环局部变量，且保留未用导入             | 2       | 使用 lambda 默认参数绑定已计算的剩余工具次数，并移除 `asyncio`；重新运行 Ruff 与定向测试                                                                          |
| P2 图实现与新增测试未满足 Ruff 格式                               | 1       | 使用项目 Ruff 自动格式化目标文件后，再运行静态检查和定向测试                                                                                                      |
| 研究图测试补丁假定了错误的 `Sequence` 导入来源                    | 1       | 先按当前文件的实际导入与类定义重新读取，再用精确锚点拆分补丁；不重复同一上下文补丁                                                                                |
| 误读为存在 `test_research_service.py`                             | 1       | 改为根据 `rg --files` 发现的实际研究服务与执行测试定位；不创建重复或脱离现有测试组织的文件                                                                            |
| 后端研究测试定位命令使用了 PowerShell 不支持的 `test_research*` 路径通配 | 1 | 改为先用 `rg --files backend/tests` 列出真实文件，再以显式文件路径读取或执行。 |
| 在 `backend` 工作目录内对 Ruff 使用了带 `backend/` 前缀的相对路径 | 1 | `ruff` 报 E902，但后续 pytest 掩盖了退出码；改用 `app/...` 与 `tests/...` 相对路径，并拆分静态检查与测试命令。 |
| 新增执行服务测试留下未使用的 `uuid4` 导入 | 1 | 删除未使用导入后单独重跑 Ruff，避免与 pytest 串联掩盖错误。 |
| 全量 Pyright 检出三个测试类型问题 | 1 | 为 P2 测试 trace 使用非空默认工厂、为图测试的路由模式补 `Literal` 属性注解，并将真实候选测试的可空来源语言回退为 `UNKNOWN`。 |
| 全量 pytest 中候选相关性单项重试未注入 P1 独立主张核验替身 | 1 | 增加显式通过的核验替身；该测试只验证单项重试成功和幂等性，不应尝试构造真实聊天模型。 |
| 在 `backend` 工作目录内再次使用了项目根目录前缀读取 compose 与 live 测试文件 | 1 | `docker compose`/`Get-Content` 找不到重复前缀路径；改为在项目根目录读取基础设施文件，在 `backend` 内使用 `tests/...` 相对路径。 |
| 新增真实治理验收脚本留下未使用的 `ResearchRun` 导入 | 1 | 删除导入后分开重跑 Ruff、Pyright 和显式 live 测试，不以末尾跳过测试掩盖静态检查失败。 |
| 真实模型返回 `router` 而不是 `mode`，导致结构化路由解析失败 | 1 | 在 `ResearchRouteDecision` 的输入规范化层仅将缺失 `mode` 时的语义等价 `router` 映射为 `mode`；保留两种合法路由值的严格约束，并补单元回归。 |
| 路由别名修复后真实模型又返回 `choice` 而不是 `mode` | 2 | 将规范化扩展为仅接受一个已观察到的 `router` 或 `choice` 别名；多个或未知别名仍由严格 schema 拒绝，随后按三次错误协议进行最后一次真实模型复验。 |
| 最后一次真实模型 RAG 专项超过 124 秒外层时限 | 3 | 不重复相同的长链路调用；审计确认测试 `finally` 已清理精确标识的 PostgreSQL 资源。P2 的真实 Worker 治理专项已独立通过；P1-C 真实 Reranker 与本次长链路复验均保持未完成，不伪称成功。 |
| 新增浏览器用例以 `getByText(..., exact: true)` 匹配被同一状态容器串联的治理摘要 | 1 | 改为断言 `.research-chat-run-status` 包含路由理由和预算摘要，匹配实际可访问文本结构。 |
| 新增入库提交时间字段未满足 Ruff 多行格式 | 1 | 使用项目 Ruff 格式器处理 `document.py` 后，定向和全量格式检查均通过。 |
| 新增 API 契约测试把 `HTTPException.detail` 直接当作字典 | 1 | 用 `cast(dict[str, object], response.detail)` 明确收窄测试断言，不改变路由响应。 |
| 讨论稿首轮补丁使用了整句之外的段落锚点 | 1 | 补丁未写入任何文件；读取当前原文后按完整段落和精确行拆分同步。 |
| 从 `backend` 工作目录对 Git diff 使用了项目根前缀 | 1 | 未影响文件；后续审阅使用根目录或 `app/...` 等正确相对路径。 |
| 读取路由源代码后仍从 `backend` 目录访问项目根规划文件 | 1 | 使用 `../task_plan.md` 与 `../progress.md` 读取根规划文件，后续命令保持工作目录与相对路径一致。 |
| Playwright 全套回归被 Vite 运行时错误覆盖层拦截                   | 1       | 移除 `PaperDetailView.vue` 中断开 `v-else-if` 链的裸 `>`；全套回归复跑为 `7 passed`                                                                                 |
| 前端目录下仍使用 `frontend/...` 路径执行 Prettier                 | 1       | 改用相对于 `frontend` 工作目录的 `src/...` 与 `tests/...` 路径；不重复原命令                                                                                        |
| 无已有规划文件                                                    | 1       | 按 `planning-with-files` 模板创建本计划、发现和进度文件                                                                                                               |
| `alembic check` 检出唯一索引与 ORM 唯一约束不一致                 | 1       | 新增 `f41c8e7b2a06` 校正迁移，不修改已应用 revision                                                                                                                   |
| `pending` 迁移找不到状态约束                                      | 1       | 历史命名约定已生成最终名；迁移对既有约束使用 `op.f()`，避免二次添加前缀                                                                                               |
| PostgreSQL `COMMENT` 不能使用绑定参数                             | 1       | 使用固定迁移文案执行本地注释同步，未更改业务数据                                                                                                                      |
| Playwright 缺少 Chromium 二进制                                   | 1       | 执行 `npx playwright install chromium` 安装锁定版本的浏览器后，认证 E2E 通过                                                                                          |
| 登录重定向 URL 断言使用 `%2F`                                     | 1       | 浏览器将查询参数展示为 `/`；测试改为验证实际序列化后的 URL                                                                                                            |
| 研究范围构造函数接收普通字符串预设                                | 1       | 将计划页的 `timePreset` 明确约束为 `ResearchTimePreset` 联合类型，恢复 `vue-tsc` 检查                                                                                 |
| 旧 FastAPI 进程未加载新增研究会话路由                             | 1       | 精确重启本地 Uvicorn 后确认 OpenAPI 和浏览器页面均可访问会话接口                                                                                                      |
| 窄屏直接隐藏研究会话侧栏                                          | 1       | 改为可打开的移动端抽屉，增加遮罩关闭和图标按钮无障碍标签                                                                                                              |
| `alembic check` 误报 LangGraph checkpoint 表应被删除              | 1       | 在 Alembic `include_object` 中排除第三方运行时表和索引                                                                                                                |
| 移动端全页截图首次捕获失败                                        | 1       | 改用视口截图完成移动端验收，并保存桌面/移动端截图产物                                                                                                                 |
| 候选相关性改动后的串行检查超时                                    | 1       | 后端 Ruff 与 Pyright 已完成；前端 typecheck 被错误地从 backend 工作目录串行启动，后续拆分到 frontend 目录并单独设置 Node 20 运行时                                    |
| 候选相关性续作的静态检查失败                                      | 1       | Ruff 报告导入顺序、行宽与格式化差异；Pyright 发现 Redis `eval` 的同步类型标注和测试替身协议签名不一致。将按静态检查的精确位置修复后再复跑，不能将其当作测试噪声忽略。 |
| Phase 12 首轮静态检查失败                                         | 1       | 新增候选审核路由时错误映射函数被插入到既有返回语句中，且 Redis 快照统计缺少字典校验；已修复后再运行 Ruff、格式化与 Pyright。                                          |
| 真实 arXiv 专项验收超过外层 64 秒时限                             | 1       | 直连下载较慢；确认 `127.0.0.1:7897` 代理可用且显著更快，专项验收仅通过显式 `FULLTEXT_NETWORK_MODE=proxy` 运行，不改变常规配置。                                       |
| 真实 arXiv 专项测试读取了不存在的 `Document.latest_ingestion_run` | 1       | `Document` 的实际关系是 `ingestion_runs`；测试改为按 `document_id` 查询 `IngestionRun` 并断言新建运行的 `pending` 状态。                                              |
| Phase 12 发布前 Ruff 检查失败                                     | 1       | 新增真实专项测试保留了未使用的 `AsyncSession` 导入，并有一行超过 100 字符；已删除导入并换行，待重新执行静态检查。                                                     |
| 发布前审计脚本首次未输出结果                                      | 1       | PowerShell 中含引号的密钥正则表达式转义失败；改为无引号的固定密钥前缀模式后，已完成差异、`.env` 跟踪和密钥扫描。                                                      |
| P0 差异审计的空匹配处理失败                                       | 1       | PowerShell 不支持 Bash 风格的 `|| exit 0`；改为检查 `$LASTEXITCODE` 并输出无匹配结果后完成审计。                                                                    |

## Notes

| 真实检索完成后重新进入工作区仍回到计划确认页 | 1 | 已在 Phase 7.2 通过当前运行恢复逻辑修复；`screening` 工作区会进入候选结果页 |

- Phase 1 至 Phase 5 已完成且已通过真实端到端验收；当前实现到“检索候选 -> 受控全文获取 -> 待确认集合 -> 用户确认构建 -> RAG 入库 -> 工作区可研究”。
- `ResearchCollectionAdmissionService`、`app.workers.ingestion` 和现有 Provider 代码应作为既有能力复用，后续 API 不得绕过其准入边界。
- Phase 7 前端实施必须复用后端返回的英文状态值和中文展示元数据，不在前端复制工作流状态字典；长任务状态以 TanStack Query 和 SSE 恢复为准。
- Phase 7 不实现 RAG 回答模拟；研究对话入口在集合可研究但后端接口未就绪时保持明确锁定状态。
- 每完成一个 Phase，都要同步更新 `task_plan.md` 和 `progress.md`；新发现写入 `findings.md`。
