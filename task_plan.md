# Task Plan: 研究入口到文献结果后端闭环

## Goal

将用户的自然语言研究要求接入后端并迁移到 Vue 3 前端，完成工作区草稿、意图分析、研究计划确认、多源异步检索、进度恢复、统一文献结果输出和集合构建，为后续 RAG 研究提供稳定入口。

## Current Phase

Phase 11：发布前审计、文档同步与版本提交（已完成）

## Current Objective

已在既有“研究计划 -> 多源检索 -> 基础初筛 -> 用户筛选 -> 全文准入”的闭环中完成可追溯的候选相关性评估 Agent。候选快照先可见，语义评估以可恢复的渐进状态补全；题录核验保持独立准入信号，不能阻塞相关性判断或被模型伪装成学术结论。

## Phases

### Phase 1: 需求与边界确认

- [x] 阅读产品、检索、RAG、数据库和前端交互讨论稿
- [x] 对齐工作区创建时机：提交非空要求时创建 `draft`
- [x] 对齐已实现能力：Provider、准入服务、RAG 入库 Worker 已存在
- [x] 明确未实现能力：检索任务 API、自动投递、SSE、意图 Agent、RAG 检索与回答
- **Status:** complete

### Phase 2: 工作流契约与状态基础

- [x] 为工作区增加独立的研究阶段字段，避免与 `active/archived` 生命周期状态混用
- [x] 为每个工作流状态补充中文代码注释、数据库字段注释和 API 展示元数据，英文状态值保持稳定
- [x] 定义研究计划契约：原始问题、候选方向、时间范围、语言、查询表达式、确认版本
- [x] 定义检索运行契约：运行标识、阶段、Provider 状态、候选计数、错误、重试和完成状态
- [x] 明确 PostgreSQL 持久状态与 Redis 短期搜索状态的边界与 TTL
- [x] 为工作流状态转换、权限和重复提交补充单元测试
- **Status:** complete

### Phase 3: 创建工作区与意图分析

- [x] 扩展创建工作区接口，接收研究要求并保存原始问题
- [x] 实现结构化意图分析适配器，输出 2-3 个候选研究方向
- [x] 校验模型输出，拒绝缺少方向、范围或语言字段的非法结果
- [x] 实现读取、修改和重新生成研究计划的接口
- [x] 实现确认研究计划接口，固化时间范围、语言和查询计划
- [x] 测试模型超时、格式错误、重复确认和用户越权场景
- **Status:** complete

### Phase 4: 多源检索任务与进度

- [x] 由确认计划创建 `search_run`，防止未确认计划直接检索
- [x] 用 arq/Redis 投递检索任务，复用现有 Provider Registry 和限速配置
- [x] 并发执行 OpenAlex、Crossref、arXiv、Semantic Scholar，并隔离单源失败
- [x] 接入现有规整、去重、初筛、题录补全和引用格式化处理链
- [x] 保存短期候选结果、来源状态和可恢复错误，不将未准入候选写入长期 `papers`
- [x] 提供检索运行详情、候选结果和重试接口
- [x] 提供 SSE 进度事件：阶段、来源状态、数量、失败原因和完成状态
- [x] 编写离线 Provider 编排测试和真实多源集成测试
- **Status:** complete

### Phase 5: 全文准入与入库任务 API

- [x] 提供候选全文获取/校验接口，只接受当前搜索运行发现的 URL
- [x] 接入现有 Open Access PDF 下载与 MinIO 暂存服务
- [x] 提供加入、移出待确认集合接口，并复用严格 DOI/全文准入服务
- [x] 确认集合后自动投递 `ingestion_run_id` 到 arq
- [x] 提供集合文献、入库阶段和可问答数量查询接口
- [x] 提供部分失败、重试和失败原因恢复接口
- [x] 验证准入事务、对象补偿清理和 Worker 投递的端到端行为
- **Status:** complete

### Phase 5 Verification

- [x] 使用真实 arXiv 开放 PDF 完成“暂存 -> 准入 -> 构建 -> 解析分块 -> 向量化 -> Milvus 写入 -> 工作区可研究”的完整闭环。
- [x] 验证状态按 `pending -> queued -> running -> completed` 推进；完成的入库运行被标记为当前版本，工作区进入 `researching`。
- [x] 验证硅基流动 `Qwen/Qwen3-Embedding-0.6B` 实际返回 1024 维向量；PostgreSQL 记录 4 个 L1、13 个 L2、47 个 L3 分块，Milvus 写入 47 个 L3 向量。
- [x] 修复异步 SQLAlchemy 会话在只读查询后留下隐式事务、导致后续写事务无法开始的问题，并补充回归测试。
- [x] 按 UUID、对象键和入库运行精确清理真实测试数据，确认 PostgreSQL、MinIO 与 Milvus 均未留下临时记录。
- **Status:** complete

### Phase 6: 端到端验证与前端接入准备

- [x] 以静态原型页面的状态需求为依据整理 OpenAPI 契约
- [x] 为工作区切换器提供名称/阶段搜索和稳定游标分页
- [x] 完成“输入要求 -> 计划确认 -> 检索启动”的 API 集成测试
- [x] 验证刷新页面后可以恢复工作区、计划和检索运行状态
- [x] 验证跨用户、跨工作区候选与全文访问隔离
- [x] 验证 Provider 部分失败、任务重试和客户端断线恢复
- [x] 更新后端开发说明和接口文档，记录本链路的启动与测试命令
- **Status:** complete

### Phase 7: Vue 前端主流程实施

- [x] 阅读静态原型和前端交互讨论稿，建立页面、路由和服务端状态映射
- [x] 创建 Vue 3 应用壳、路由、TanStack Query 客户端、Pinia 轻量状态和统一 API 客户端
- [x] 实现登录、注册、当前用户和鉴权失效恢复
- [x] 实现研究入口：提交非空要求、分析状态、计划候选、时间范围/语言编辑和计划确认
- [x] 实现检索进度：来源状态、候选数量、失败原因、重试和 SSE 断线恢复
- [x] 实现结果表、论文详情、引用复制、全文获取、加入/移出待确认集合
- [x] 实现集合确认构建、入库阶段、部分失败和页面刷新恢复
- [x] 实现工作区切换器的关键词搜索、游标分页和当前阶段跳转
- [x] 为候选题录补充正式引用格式 API，并在详情页提供 GB/T、APA、MLA、Chicago 与 BibTeX 预览、复制
- [x] 为关键页面添加组件/组合式函数单元测试和 Playwright 主流程测试
- [x] 在真实 FastAPI、PostgreSQL、Redis 和 Worker 环境验证前端主流程
- **Status:** complete

### Phase 7.1: 原型交互流程对齐

- [x] 新增连续研究画布，将任务解析、计划确认和检索推进保留为一个用户可感知的流程
- [x] 将工作区阶段栏收敛为任务解析、文献筛选和证据研究三个主阶段
- [x] 移除会绕过当前阶段的侧栏平级导航，工作区切换只进入目标工作区的当前阶段
- [x] 重组候选筛选页，补齐处理明细、候选检查器和确认构建集合的明确边界
- [x] 更新前端路由映射、单元测试与 Playwright 主流程测试，并在 API 契约模拟环境验证
- **Status:** complete


### Phase 7.2: 真实联调状态修复

- [x] 修复重新进入工作区时未恢复当前检索运行、错误回到计划确认页的问题
- [x] 修复检索运行画布读取到过期候选统计、完成后仍显示 0 条候选的问题
- [x] 修复未就绪题录被错误显示为“正在渲染”的永久加载状态
- [x] 将全文 `rejected` 识别为终态，并在未满足题录准入时禁用全文请求入口
- [x] 为上述状态分支补充前端测试，并用真实 FastAPI 环境复验已有工作区
- [x] 复跑浏览器真实回归，确认移除无全文状态探测后不再产生 404 控制台错误
- **Status:** complete
### Phase 8: RAG 研究会话后端

#### Phase 8.1: 会话、运行与接口契约

- [x] 审计既有 `conversations`、`messages`、`research_runs`、`research_evidences` 表与已应用迁移，补齐仅在实际 API 需要时才存在的约束、索引和中文注释
- [x] 定义会话、消息、研究运行、执行阶段、证据引用与“证据不足/需要澄清”Pydantic 契约；机器状态保持英文稳定值，接口提供中文展示元数据
- [x] 设计会话创建、列表、读取、提交问题、读取运行、重试和取消 API；所有资源均通过工作区所有权与集合范围校验
- [x] 明确 PostgreSQL 业务运行、LangGraph checkpoint、Redis 进度流各自保存的状态，避免把恢复能力放入进程内存或消息 JSON
- [x] 为权限隔离、重复提交、运行状态转换、会话删除与输入校验编写单元及 API 契约测试
- **Status:** complete

#### Phase 8.2: 受限混合检索与证据组装

- [x] 扩展现有 OpenAI 兼容 embedding 适配器，支持查询向量且校验其维度与当前 Milvus collection 一致
- [x] 实现检索仓储：先由 PostgreSQL 确定当前 `completed/is_current` 文档版本，再以 `owner_user_id + collection_id + ingestion_run_id + level=3` 过滤 Milvus；读取原文时再次校验集合和版本
- [x] 实现关键词召回、向量召回、RRF 融合、同文献父块 Auto-merging 与可配置的相关性阈值；先以可审计的确定性算法落地，再只在有真实评估数据时引入 Reranker
- [x] 为每个候选保存检索阶段、分数、父块合并原因、页码/章节定位和最终引用片段；不把检索分数误表示为事实正确性
- [x] 用内存替身和本地 Milvus/PostgreSQL 验证跨用户、跨集合、旧文档版本和无可检索文献均无法泄漏内容
- **Status:** complete

#### Phase 8.3: 单轮证据回答 LangGraph 与异步运行

- [x] 实现 `single_rag` LangGraph：问题规范化 -> 受限检索 -> 证据充分性判断 -> 带引用回答 / 一次改写 / 澄清 / 拒答
- [x] 查询改写最多一次；HyDE 或 Step-back 仅用于检索，不能成为引用或答案事实来源
- [x] 以结构化模型输出约束回答引用只能来自本次 `research_evidences`；每条事实、方法、结果或结论均关联论文信息、原文摘录和页码或章节定位
- [x] 通过 arq 独立研究 Worker 执行运行，持久化 `queued -> running -> awaiting_clarification/completed/failed/cancelled`，向 Redis Stream 发布可展示阶段事件
- [x] 接入 LangGraph PostgreSQL checkpoint，实现 Worker 重启后的同一运行恢复；checkpoint 不替代 `research_runs` 业务审计记录
- [x] 对模型、嵌入、Milvus、数据库与 checkpoint 失败建立稳定错误码、可重试规则和取消边界
- **Status:** complete

#### Phase 8.4: 复杂研究问题与证据核验

- [x] 为跨论文比较、冲突分析和结构化研究笔记定义受限的复杂问题判定契约、子问题上限、并发上限、超时与总模型预算
- [x] 使用 Plan-and-Solve 生成有限子问题；每个子问题只可调用 Phase 8.2 的集合受限检索
- [x] 增加证据核验步骤，拒绝片段不蕴含、扩大解释和无法定位的原子事实；综合节点只能消费已核验事实
- [x] 使用受限 ReAct 工具循环，工具仅限检索与读取已授权证据；达到工具或改写预算后必须结束为回答、澄清或证据不足
- [x] 保存子问题、检索 trace、核验结论和未解决缺口，支持部分成功但不允许其他子问题静默猜测补齐
- **Status:** complete

#### Phase 8.5: 真实端到端验收与质量基线

- [x] 使用真实已准入、已向量化的论文集合完成单篇问答和证据不足离线图测试；跨论文比较、部分入库失败与刷新恢复仍需多文献真实样本
- [x] 核验每个最终引用都能回到同一用户、同一集合、当前文档版本的 PostgreSQL 片段，并可显示题名、作者/年份、链接及页码或章节
- [~] 验证 Worker 重启、模型/向量服务瞬时失败、重复投递、取消、无权限访问和 Redis 事件断线恢复（已验证无权限访问、Redis `Last-Event-ID` 断线恢复、队列重复投递的幂等代码路径；Worker 重启、显式取消和模型/向量瞬时失败仍需专项真实故障注入）
- [x] 记录首批检索命中、引用可定位和证据支撑人工验收样本；单轮基线已满足启用后续复杂模式的代码条件
- **Status:** in_progress

### Phase 9: Vue 研究对话页接入

- [x] 将集合页的对话入口仅在当前集合可问答文献数大于零时解锁，并传递服务端工作区与集合标识
- [x] 接入真实会话列表、创建会话、消息流、运行状态和 SSE 断线恢复；不在前端伪造研究回答
- [x] 在每条回答下展示可展开证据、论文题名、作者/年份、原文摘录和页码或章节定位；证据不足和澄清属于正常可见状态
- [x] 实现当前工作区内会话历史、明确的范围切换、刷新恢复和会话删除确认；工作区切换不与会话侧栏相互挤占
- [x] 通过真实 FastAPI、Redis、Worker、PostgreSQL 和 Milvus 浏览器回归，覆盖引用定位、越权拒绝、长任务恢复和移动端可读性
- **Status:** complete

### Phase 10: 候选相关性评估 Agent

#### Phase 10.1: 评估契约与边界

- [x] 定义后端唯一的 `CandidateRelevanceAssessment` 契约，包含相关层级、自然语言理由、研究内容概述、可帮助的研究环节、局限、建议和逐条证据。
- [x] 将相关层级限定为 `core`（核心相关）、`related`（关联研究）、`background`（背景参考）、`not_recommended`（不建议优先）和 `insufficient_information`（信息不足）；这些不是论文质量分数，也不替代用户最终决定。
- [x] 证据仅允许引用候选的标题或摘要；每条证据记录来源字段、原文摘录和可选的字符位置。服务端必须验证摘录确实来自对应原文，拒绝模型编造的引文。
- [x] 明确摘要缺失、题录冲突或模型无法判断时的行为：返回“信息不足”或明确失败，绝不回退为前端关键词推测。
- [x] 在 `UnifiedCandidate` 的 Redis 短期快照中保存评估结果与状态；评估数据不写入长期论文表。仅为公开搜索运行阶段新增约束迁移，不建立候选评价表。
- **Status:** complete

### Phase 11: 发布前审计、文档同步与版本提交

- [x] 恢复并审查 Phase 10 的统一候选、Redis 快照、模型契约、API/SSE 与前端呈现边界，确认候选理由没有回退到前端关键词推断。
- [x] 核对讨论稿、环境模板与后端开发说明均记录“规整初筛 -> 候选快照 -> 相关性评估 -> 高相关题录预取”的真实处理顺序。
- [x] 复跑后端、迁移与前端全量静态/测试验证；所有命令须使用项目锁定的 Python、Node 20.19.6 与 pnpm 10.34.5。
- [x] 复查完整暂存差异、暂存迁移文件的最终格式和空白字符，确认没有真实密钥、临时数据或未说明的行为变更。
- [x] 创建包含研究工作流、RAG 对话、Vue 接入和候选相关性评估的本地 Git 提交；提交标识以最终 `git log -1` 为准。
- **Status:** complete

#### Phase 10.2: 检索 Worker 中的评估阶段

- [x] 在多源规整、去重和规则初筛完成后，先发布带“待评估”状态的候选快照，再为 `triage.included=true` 且具备摘要的候选增加“相关性评估中”阶段；缺摘要候选由确定性规则标为信息不足，未通过基础初筛的记录不消耗模型调用。
- [x] 复用现有 DeepSeek/OpenAI 兼容 LangChain 模型适配器，以受控批次评估候选；输入只包含用户原始问题、已确认方向、时间/语言范围、实际检索词以及候选元数据。
- [x] 对单次输入的候选数量、摘要长度、批次并发、超时和最大输出长度设定明确上限，避免 50 条以上候选导致不可控成本或模型上下文溢出。
- [x] 使用结构化输出和服务端二次校验：返回候选 ID 必须完整且唯一；层级、证据字段、证据摘录和推荐文案均须符合契约。
- [x] 允许批次级部分失败：已完成候选保持结果，失败候选显示明确失败原因并可针对失败项重试；不因为单个来源或模型调用失败而覆盖已有候选。
- [x] 将 Search Run 的公开阶段、Redis Stream 事件和恢复快照同步扩展到该阶段，使刷新页面或 SSE 断线后可看到已评估、待评估和失败数量；候选结果页在快照可用时进入，不等待整轮模型调用结束。
- [x] 将当前“检索完成前批量题录补全”的实现改为非阻塞策略：仅对 Agent 已判定为核心/关联的少量候选预取题录，其余在用户打开详情、复制引用或加入集合时按需处理。
- **Status:** complete

#### Phase 10.3: API 与短期状态恢复

- [x] 扩展候选结果 API，使其直接返回服务端评估结果、评估状态和可展示错误；前端不再自行推断相关性。
- [x] 设计失败候选的授权重试 API，只允许当前工作区所有者、当前搜索运行且处于可重试状态的候选触发；重复请求必须幂等。
- [x] 维持现有 Redis TTL 边界：候选及评估随搜索会话过期；过期后提示重新检索，不把模型评价错误地当作长期学术结论保存。
- [x] 确认候选被加入研究集合时仅沿用既有 DOI、正式题录和全文准入边界；相关性评估只能辅助排序与人工筛选，不能绕过准入。
- **Status:** complete

#### Phase 10.4: 前端理由呈现与旧规则退役

- [x] 删除生产路径对 `candidate-reason.ts` 关键词匹配规则的依赖，确保候选理由只有一个服务端事实来源。
- [x] 列表卡片只展示层级标签和一句“为什么保留”的自然语言摘要；不显示虚假的百分比、命中次数或模型内部技术字段。
- [x] 右侧检查器按“它主要研究什么 / 对当前研究有什么帮助 / 需要留意 / 判断依据 / 建议”展示评估结果，并让标题、摘要证据可展开核对。
- [x] 对评估中、信息不足和评估失败分别提供清晰的中文状态；失败状态提供显式重试，不伪造关键词理由作为降级内容。
- [x] 保留用户自主选择：低优先级与不建议优先的候选默认不自动入选、不自动删除，用户可按优先审核、背景参考和需人工核对筛选后决定。
- **Status:** complete

#### Phase 10.5: 质量验证与真实验收

- [x] 编写契约和服务单元测试，覆盖模型输出缺字段、候选 ID 重复、证据不属于原文、无摘要、批次部分失败和重试幂等。
- [x] 编写搜索 Worker/API 集成测试，覆盖 Redis 快照恢复、SSE 进度、跨用户隔离、搜索会话过期和现有全文准入不被评估结果绕过。
- [x] 更新前端单元与 Playwright 测试，验证理由来自后端、证据可查看、失败不会显示伪理由、筛选不会改变用户选择结果。
- [x] 使用显式环境变量完成 DeepSeek 结构化评估验收，并以受控的真实多源检索验证候选渐进评估；模型理由证据均可回到候选标题或摘要，临时数据库和 Redis 数据已清理。
- [x] 完成后运行后端测试、Ruff、Pyright、Alembic check，以及前端 typecheck、lint、unit、build 和浏览器主路径回归。
- **Status:** complete

## 2026-08-02 实施对齐

已落地并待真实验收的后端实现：`c3e7a1b9d426` 迁移、研究会话 API、受限混合检索、LangGraph、ARQ 研究 Worker、Redis Stream 和 PostgreSQL checkpoint。Phase 8.1 至 8.4 的代码和离线测试均已完成；本轮以 Phase 8.5 和 Phase 9 的真实验收及前端接入为执行范围。

- [x] 保持已区分的“搜索运行”和“研究运行”类型边界，并增加独立 `/research/:workspaceId` 路由。
- [x] 实现集合页解锁入口、研究会话侧栏、问题提交、SSE 阶段恢复、回答证据展开和删除确认。
- [x] 完成前端静态检查、后端完整检查，以及真实 PostgreSQL、Redis、Milvus、ARQ、模型的单篇验收；复杂模式保留真实多文献故障基线待补。

已完成真实单篇验收：`RUN_LIVE_RESEARCH_E2E_TESTS=1 uv run pytest tests/integration/test_live_research_e2e.py -m live -s` 通过。真实 Qwen embedding 写入 Milvus 后，受限检索命中 2 个 L3 片段，DeepSeek 生成带 2 条可回链引用的回答，Redis 产生 5 个公开阶段事件；临时 PostgreSQL、Milvus、Redis 与 checkpoint 资源均已清理。复杂模式和证据不足分支已有离线图测试，待后续真实多论文集合基准验证。

2026-08-02 本轮补充真实 API + arq 验收：临时账号经 FastAPI 提交研究要求，DeepSeek 意图分析生成 3 个方向；确认计划后 workflow Worker 实际完成 OpenAlex、Crossref、arXiv 检索，API 返回 51 条候选、50 条纳入候选和 25 条题录补全。另一条临时已索引集合经研究会话 API 投递问题，research Worker 实际完成 Qwen 查询嵌入、Milvus 受限召回和 DeepSeek 回答；最终 API 返回 `completed`、2 条可定位证据，Redis Stream 产生 5 条公开事件且 SSE 可返回初始快照。全文入库真实验收同时确认 arXiv PDF -> MinIO -> 47 个 L3 分块 -> 47 条 1024 维 Milvus 向量 -> `researching` 状态闭环。所有临时资源均已清理。

## Key Questions

1. 研究计划和检索运行中哪些字段需要长期写入 PostgreSQL，哪些只在 Redis TTL 内保留？
2. 工作区研究阶段是新增 `workflow_stage` 字段，还是使用独立的工作流运行表承载？
3. 意图分析首版使用一次结构化 OpenAI 兼容模型调用，还是直接用 LangGraph `SearchGraph` 封装？
4. 搜索候选在 Redis 过期后，前端应提示重新检索，还是将候选结果持久化为可恢复的搜索快照？
5. 前端首版是否只支持桌面端完整工作流，还是同步保证移动端的结果查看与状态恢复？

## Decisions Made

| Decision | Rationale |
|----------|-----------|
| 先实现“入口到结果”垂直闭环 | 前端已有连续流程，Provider 适配器已完成，缺的是可恢复的后端编排 |
| 提交要求时先创建 `draft` 工作区 | 与最新前端交互稿一致，保证离开页面后仍有任务边界 |
| 意图分析输出结构化研究计划 | 用户需要确认方向、时间和语言，检索不能直接依赖自然语言模型文本 |
| 状态值英文、说明中文 | 英文值适合作为数据库/API 稳定标识；中文注释和展示元数据帮助开发者与用户理解状态 |
| Provider 调度和任务状态由确定性代码负责 | 限速、重试、权限和状态需要可预测、可审计，不能交给 Agent 自主循环 |
| 未通过准入的候选只保留短期搜索状态 | 数据库长期 `papers` 只保存 DOI、正文和权限均已核验的文献 |
| RAG 检索与研究对话放在本链路之后 | 必须先有可研究集合和完成的向量索引，避免提前实现无数据可用的聊天接口 |
| 检索候选详情留在 Redis | `search_runs` 只持久化运行状态、统计、错误与 Redis 会话键，过期候选可由确认计划重新生成 |
| 入库 Worker 同一 `AsyncSession` 在只读后无法开启写事务 | 1 | 在仓储层将查询结果转换为值对象后结束只读事务，使后续状态与向量记录写入拥有明确事务边界 |
| 前端主流程需要等待 RAG 对话 API | 研究集合构建已有完整后端接口，但研究检索、回答和证据 trace 尚未提供；先实施前置流程，避免前端伪造研究回答 |
| 候选引用格式需要专用 API | 后端已有格式化器但没有候选题录渲染端点；前端不能手拼格式或把标题加 DOI 当作正式引用 |
| 前端必须将题录与全文准入失败清楚呈现 | `citation_not_ready` 是既定业务门槛，不应以永久加载或请求按钮掩盖服务端拒绝结果 |
| 候选相关性与研究准入分离 | Agent 只判断标题/摘要与研究意图的关系；DOI、题录和全文状态是确定性准入信号，二者同时展示但不能互相覆盖 |
| 候选快照优先于语义评估完成 | 用户不应等待全部模型调用才能看到文献；Redis 快照和 SSE 逐批更新，前端只呈现服务端状态 |

## Errors Encountered

| Error | Attempt | Resolution |
|-------|---------|------------|
| 无已有规划文件 | 1 | 按 `planning-with-files` 模板创建本计划、发现和进度文件 |
| `alembic check` 检出唯一索引与 ORM 唯一约束不一致 | 1 | 新增 `f41c8e7b2a06` 校正迁移，不修改已应用 revision |
| `pending` 迁移找不到状态约束 | 1 | 历史命名约定已生成最终名；迁移对既有约束使用 `op.f()`，避免二次添加前缀 |
| PostgreSQL `COMMENT` 不能使用绑定参数 | 1 | 使用固定迁移文案执行本地注释同步，未更改业务数据 |
| Playwright 缺少 Chromium 二进制 | 1 | 执行 `npx playwright install chromium` 安装锁定版本的浏览器后，认证 E2E 通过 |
| 登录重定向 URL 断言使用 `%2F` | 1 | 浏览器将查询参数展示为 `/`；测试改为验证实际序列化后的 URL |
| 研究范围构造函数接收普通字符串预设 | 1 | 将计划页的 `timePreset` 明确约束为 `ResearchTimePreset` 联合类型，恢复 `vue-tsc` 检查 |
| 旧 FastAPI 进程未加载新增研究会话路由 | 1 | 精确重启本地 Uvicorn 后确认 OpenAPI 和浏览器页面均可访问会话接口 |
| 窄屏直接隐藏研究会话侧栏 | 1 | 改为可打开的移动端抽屉，增加遮罩关闭和图标按钮无障碍标签 |
| `alembic check` 误报 LangGraph checkpoint 表应被删除 | 1 | 在 Alembic `include_object` 中排除第三方运行时表和索引 |
| 移动端全页截图首次捕获失败 | 1 | 改用视口截图完成移动端验收，并保存桌面/移动端截图产物 |
| 候选相关性改动后的串行检查超时 | 1 | 后端 Ruff 与 Pyright 已完成；前端 typecheck 被错误地从 backend 工作目录串行启动，后续拆分到 frontend 目录并单独设置 Node 20 运行时 |
| 候选相关性续作的静态检查失败 | 1 | Ruff 报告导入顺序、行宽与格式化差异；Pyright 发现 Redis `eval` 的同步类型标注和测试替身协议签名不一致。将按静态检查的精确位置修复后再复跑，不能将其当作测试噪声忽略。 |

## Notes
| 真实检索完成后重新进入工作区仍回到计划确认页 | 1 | 已在 Phase 7.2 通过当前运行恢复逻辑修复；`screening` 工作区会进入候选结果页 |

- Phase 1 至 Phase 5 已完成且已通过真实端到端验收；当前实现到“检索候选 -> 受控全文获取 -> 待确认集合 -> 用户确认构建 -> RAG 入库 -> 工作区可研究”。
- `ResearchCollectionAdmissionService`、`app.workers.ingestion` 和现有 Provider 代码应作为既有能力复用，后续 API 不得绕过其准入边界。
- Phase 7 前端实施必须复用后端返回的英文状态值和中文展示元数据，不在前端复制工作流状态字典；长任务状态以 TanStack Query 和 SSE 恢复为准。
- Phase 7 不实现 RAG 回答模拟；研究对话入口在集合可研究但后端接口未就绪时保持明确锁定状态。
- 每完成一个 Phase，都要同步更新 `task_plan.md` 和 `progress.md`；新发现写入 `findings.md`。
